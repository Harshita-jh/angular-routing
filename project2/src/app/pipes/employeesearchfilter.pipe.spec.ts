import { EmployeesearchfilterPipe } from './employeesearchfilter.pipe';

describe('EmployeesearchfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new EmployeesearchfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
